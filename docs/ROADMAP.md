# 📍 ROADMAP.md · SceneGo 项目总览（SSOT）

> **docs/ 唯一入口文件。** 规划内容见 [`PLAN.md`](PLAN.md)，详细文档位于 [`reference/`](reference/) 子目录。

## ⚠️ 给所有 Agent 的声明（必读）

**本文件只记录「正在做的」；已完成的能力由 git 版本管理记录，不在此维护。**

1. **查进度**：看本文件；查已完成的能力 → 看 git 历史。
2. **改进度**：开始做 → 加入本表；做完 → 从本表删除并提交（git 即记录）。
3. **状态图例**：🔄 进行中 · 🔶 部分完成。
4. **维护节奏**：开始/完成时更新，不设模糊的"推进中"。

---

## 一、产品概述

面向**全球出境用户**：**用户主动触发**（拍照 / 语音 / 文字），App 识别当前场景并生成**符合该场景的当地语言表达卡**，让用户能立即把需求/情境有效地传达给当地人（司机、店员、警察、海关等）。

数据基础：10 类场景词库 + 26 国安全信息（版本化场景包，离线可用）。核心承诺：**秒级成卡、离线可用、精准表达**。

## 二、正在做的

| 要做的功能 | 状态 | 说明 |
|---|---|---|
| **V3.1 交互定稿** | 🔄 设计中 | 三帧架构（输入→理解→交付）+ 双输出（Insight 只读 / Action Card 三子态）。8 屏画布已重绘，规格在 `reference/DESIGN-v3.1-interaction.md` |
| 机场场景推荐 | 🔄 设计中 | 到机场自动推荐：打车 / 末班车 / 买流量卡 |
| 打车 · 听懂司机回应 | 🔄 设计中 | 司机说话实时翻译成中文，听不懂还能「换句话重说」 |
| 点菜 · 菜单解读 | 🔄 设计中 | 拍菜单 → 招牌菜 / 过敏预警 / 辣度，点哪个菜出哪张卡 |
| 买药 · 分步引导 | 🔄 设计中 | 三步：说症状 → 说过敏 → 问用法用量 |
| 紧急电话 · 二次确认 | 🔄 设计中 | 拨 191/1669 前先弹确认，防止误触误拨 |

**为什么做这些（现在的短板）**：卡片只能递给当地人看，对方一开口就卡壳；拍菜单只出一张菜卡，还是不知道点什么；到了机场/深夜 App 一片空白没人引导；买药退税这种多步骤的事一张卡说不清。

## 三、文档导航

| 文件 | 内容 |
|---|---|
| [`PLAN.md`](PLAN.md) | **规划**：未开始的能力 + 里程碑 |
| `reference/DESIGN.md` | 设计规范 V2（对话优先架构，含界面红线） |
| `reference/DESIGN-v3.md` | V3 初稿（红色扳机等已废弃设计的记录） |
| `reference/DESIGN-v3.1-interaction.md` | **V3.1 交互定稿**：三帧架构 + 8 屏 + 双输出 + 走查 |
| `reference/DESIGN-v2.1.pen` | V2 设计画布（21 屏） |
| `reference/screens/` | UI 截图（13 张） |

**阅读顺序**：本文件（进度）→ `PLAN.md`（规划）→ `reference/DESIGN.md`（设计施工）。

---

## 版本关系

- 第一版概念草图 → 第二版对话式 UI（当前 App）→ 正在做的实用度突破（见「二、正在做的」）→ 远期构思（未动）

## 维护示例（给 Agent 的操作范式）

```
场景：某能力开始做 / 做完
动作：
  1. 开始做 → 加入「二、正在做的」；做完 → 从该表删除并提交（git 记录完成）
  2. 删除被替代的过程文档（git 历史可查）
  3. 提交信息遵循 Conventional Commits：docs(roadmap): XXX 完成
```
